export enum PathEnums {
  MAIN = "/",
  CART = "/cart",
  FAVORITES = "/favorites",
  ITEM = "/item/:itemId",
  ALL_PRODUCTS = "/all-products",
  ORDERS = "/orders",
  SUBCARDS = "/:categoryId/:subCategoryId",
}
